
<div class="row">
  <div class="small-12 columns">
  
    <h1>Node Dependency Check</h1>
    <p>Check your project against the latest npm modules</p>
    <p>Checks dependencies in <code>package.json</code>, alternatively you can add a <code>.dependencies.json</code> file following the same format as package.json's dependencies section</p>
    
    {{#if message}}
      <div class="alert-box alert">{{message}}</div>
    {{/if}}
    
    <form action="/check" method="POST">
      
      <h4>Select from:</h4>

      {{#if repos.length}}
        <select name="repo">
          {{#each repos}}
            <option value="{{full_name}}">{{full_name}}</option>
          {{/each}}
        </select>
      {{else}}
        <a href="/auth/github" class="secondary button ghbutton"><i class="fa fa-github"></i><span>Github</span></a>
      {{/if}}
      
      <h4 class="text-center">OR</h4>

      <div class="row inline">
        <div class="small-12 columns">
          <label for="git">git repo clone URL:</label>
          <input type="text" name="git-repo" id="git" placeholder="e.g https://github.com/petecoop/npm-check-latest.git">
        </div>
        
        <h4 class="text-center">OR</h4>
        
        <div class="small-12 columns">
          <label for="paste">Paste your <code>package.json</code>:</label>
          <textarea name="pasted" rows="5"></textarea>
        </div>
        
        <div class="small-12 columns">
          <button type="submit" class="success">Go</button>
          <input type="checkbox" name="dev" id="dev" value="1" checked>
          <label for="dev">Check Dev Dependencies</label>
        </div>
      </div>
    </form>

  </div>
</div>